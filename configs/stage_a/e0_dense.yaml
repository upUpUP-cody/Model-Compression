# E0 Dense baseline — formal Qwen2.5-3B-Instruct
experiment_id: E0
model:
  path: /mnt/data/models/Qwen2.5-3B-Instruct
  pdf_model: Qwen2.5-3B-Instruct
  spec: formal_3B
  torch_dtype: float16

hardware:
  device: cuda:0
  batch_size: 1

evaluation:
  max_seq_len: 256
  validation_max_samples: 64
  train_max_samples: 64

logging:
  output_root: /mnt/data2/results/E0_dense_baseline

seed: 42

dataset:
  name: nyu-mll/glue
  task: sst2
  cache_dir: /mnt/data/datasets/glue
  validation_fraction: 0.1
  split_seed: 42
